# 961. N-Repeated Element in Size 2N Array (Easy)

You are given an integer array `nums` of size `2n` that contains `n + 1` unique elements.

Exactly one element is repeated `n` times, while the remaining elements appear only once.

Return the element that is repeated `n` times.
